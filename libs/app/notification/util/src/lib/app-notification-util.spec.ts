import { appNotificationUtil } from './app-notification-util';

describe('appNotificationUtil', () => {
  it('should work', () => {
    expect(appNotificationUtil()).toEqual('app-notification-util');
  });
});
