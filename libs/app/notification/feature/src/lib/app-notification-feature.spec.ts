import { appNotificationFeature } from './app-notification-feature';

describe('appNotificationFeature', () => {
  it('should work', () => {
    expect(appNotificationFeature()).toEqual('app-notification-feature');
  });
});
