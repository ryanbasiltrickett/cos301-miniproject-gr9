import { appNotificationUi } from './app-notification-ui';

describe('appNotificationUi', () => {
  it('should work', () => {
    expect(appNotificationUi()).toEqual('app-notification-ui');
  });
});
