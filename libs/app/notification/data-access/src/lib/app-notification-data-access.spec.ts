import { appNotificationDataAccess } from './app-notification-data-access';

describe('appNotificationDataAccess', () => {
  it('should work', () => {
    expect(appNotificationDataAccess()).toEqual('app-notification-data-access');
  });
});
