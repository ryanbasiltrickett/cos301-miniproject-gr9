import { appForgetDataAccess } from './app-forget-data-access';

describe('appForgetDataAccess', () => {
  it('should work', () => {
    expect(appForgetDataAccess()).toEqual('app-forget-data-access');
  });
});
