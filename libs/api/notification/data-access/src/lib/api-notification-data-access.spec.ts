import { apiNotificationDataAccess } from './api-notification-data-access';

describe('apiNotificationDataAccess', () => {
  it('should work', () => {
    expect(apiNotificationDataAccess()).toEqual('api-notification-data-access');
  });
});
